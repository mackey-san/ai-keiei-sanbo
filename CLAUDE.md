# CLAUDE.md

このファイルは、本リポジトリで作業する Claude Code へのガイドです。

## プロジェクト概要

- プロジェクト名: ai-keiei-sanbo（AI経営参謀）
- リポジトリ: https://github.com/mackey-san/ai-keiei-sanbo
- 現状: 初期セットアップ段階（ソースコードはまだありません）

技術スタック・ディレクトリ構成・ビルド／テスト手順が決まり次第、このファイルに追記してください。

## Git運用ルール

- **コードを変更するたびに、コミットして GitHub（`origin`）へプッシュすること。**
  - 1つのまとまった変更ごとにコミットし、作業の最後に変更を残したままにしない。
  - プッシュ先: `origin`（https://github.com/mackey-san/ai-keiei-sanbo.git）
- デフォルトブランチは `main`。
- コミットメッセージは変更内容が分かる簡潔な日本語または英語で書く。
- コミット前に `git status` / `git diff` で意図しないファイル（秘密情報、`.env`、ビルド成果物など）が含まれていないか確認する。
- 強制プッシュ（`git push --force`）や履歴の書き換えは、ユーザーの明示的な指示がない限り行わない。

## 注意事項

- APIキー・パスワード等の秘密情報はリポジトリにコミットしない（`.env` などで管理し `.gitignore` に追加する）。
- 本リポジトリは **Public** です。公開して問題ない内容のみをコミットすること。
